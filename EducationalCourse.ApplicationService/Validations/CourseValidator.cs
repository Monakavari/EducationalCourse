using EducationalCourse.Common.Dtos;

namespace EducationalCourse.ApplicationService.Validations
{
    public static class CourseValidator
    {
        public static void SetValidator(AddCourseDto request)
        {
            //if (string.IsNullOrWhiteSpace(request.CourseTitle) ||
              //if (request.CourseGroupId.hasva) 
               //string.IsNullOrWhiteSpace(request.RePassword) ||
               //string.IsNullOrWhiteSpace(request.Mobile))

               // throw new AppException("پر کردن فیلدهای مربوطه اجباریست.");
        }
    }
}
